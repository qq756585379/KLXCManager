<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.klxc.dao.UserCoachMapper">

	<insert id="addUserCoach" parameterType="klxc.usercoach.UserCoach">
		insert into users_coach
		(user_name, user_password, user_nicname, user_address, 
		telephone, user_image, coach_id,  source_id, user_type)
		values
		(#{user_name},#{user_password},#{user_nicname},#{user_address}
		,#{telephone},#{user_image},#{coach_id},#{source_id},#{user_type})
	</insert>
	
	
</mapper>